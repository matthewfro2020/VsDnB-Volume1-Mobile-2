package
; polymod.hscript._internal
; /** * Stub macro so engines that compile macro hooks do not fail. * No macro logic is executed.
*/ class HScriptedClassMacro
{ macro public static function build()
{ return macro
{
}
; }
}
